package algorithms;

import datastructures.Intersection;

public class platoonDispersion {

	/*public int queueGreen(int phase, Intersection i, int cycle, int interval, int time) {
		int qg = 0;
		qg = queueGreen(phase,i,cycle-1,interval,time) + (estimationGreen(phase,i,cycle,interval,time)/time)*lengthGreen(cycle,i,phase)-outgoingFlow(phase,i,cycle,interval,time);
		return qg;
	}
	
	public int queueRed(int phase, Intersection i, int cycle, int interval, int time) {
		int qr = 0;
		qr = queueRed(phase,i,cycle-1,interval,time) + (estimationRed(phase,i,cycle,interval,time)/time)*lengthRed(cycle,i,phase);
		return qr;
	}
	
	public int estimationGreen(int phase, Intersection i, int cycle, int interval, int time) {
		int eg = 0;
		return eg;
	}
	
	public int estimationRed(int phase, Intersection i, int cycle, int interval, int time) {
		int er = 0;
		return er;
	}
	
	public int lengthGreen(int cycle, Intersection i, int phase) {
		int lg = 0;
		return lg;
	}
	
	public int lengthRed(int cycle, Intersection i, int phase) {
		int lr = 0;
		return lr;
	}
	
	public int lengthCycle(int cycle, Intersection i) {
		int lc = 0;
		return lc;
	}*/
}
